const authService = require('./auth.services');
const userService = require('./user.service');
const conversationService = require('./conversation.service');
const categoryTypeService = require('./category-type.service');

module.exports = {
    authService,
    userService,
    conversationService,
    categoryTypeService
}