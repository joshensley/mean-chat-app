const authDb = require('./auth.db');
const userDb = require('./user.db');
const conversationDb = require('./conversation.db');
const categoryTypeDb = require('./category-type.db');

module.exports = {
    authDb,
    userDb,
    conversationDb,
    categoryTypeDb
}